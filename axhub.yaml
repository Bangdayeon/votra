# Generated by axhub init on 2026-05-13
version: 1
app:
    name: votra
    slug: votra
    subdomain: votra
    domain_id: 1
    framework: nextjs
runtime:
    port: 3000
    health_check: /
build:
    install: pnpm install --frozen-lockfile
    build: pnpm build
    start: pnpm start
deploy:
    target: auto
    domain_id: 1
    environments:
        - staging
        - prod
