# axhub 배포 설정
# axhub backend가 next.config.* + "next" 의존성을 자동으로 감지해요.
name: haema
subdomain: haema
domain_id: 1
runtime:
    port: 3000
    health_path: /
build:
    install: npm install
    build: npm run build
    start: npm start
