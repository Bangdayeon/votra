export type DefaultCommand = {
  slug: string;
  name: string;
  description: string;
  folder: string;
  content: string;
};

export const DEFAULT_COMMANDS: DefaultCommand[] = [
  {
    slug: "review",
    name: "코드 리뷰",
    description: "변경 사항을 체크리스트(보안, 성능, 가독성)로 구조화하여 리뷰",
    folder: "코드 품질",
    content: `## 코드 리뷰

변경된 코드를 아래 순서로 리뷰해요.

### 1. 정확성
- 요구사항을 올바르게 구현했나요?
- 엣지 케이스(빈 값, null, 경계값)를 빠뜨린 건 없나요?
- 에러 경로가 명시적으로 처리됐나요?

### 2. 보안
- 사용자 입력을 그대로 SQL/HTML에 삽입하진 않나요?
- 인증·인가 체크가 빠진 엔드포인트는 없나요?
- 비밀 키·토큰이 코드나 로그에 노출되진 않나요?

### 3. 성능
- 루프 안에서 쿼리를 반복 실행(N+1)하진 않나요?
- 대용량 데이터를 한 번에 메모리에 올리진 않나요?
- 불필요한 re-render나 연산이 있나요?

### 4. 가독성 & 유지보수
- 이름만 봐도 의도를 알 수 있나요?
- 함수가 하나의 책임만 갖나요?
- 중복 로직이 세 번 이상 반복되진 않나요?

### 5. 타입 & 일관성
- \`any\` 타입이 있나요? \`unknown\`으로 교체하고 좁혀주세요.
- 프로젝트 코딩 컨벤션과 일치하나요?

각 항목에서 발견한 문제는 **심각도(높음/중간/낮음)**와 함께 구체적인 수정 방법을 제안해요.
문제가 없으면 "이상 없음"으로 표시해요.`,
  },
  {
    slug: "refactor",
    name: "리팩토링",
    description: "동작은 유지하면서 구조, 중복, 가독성을 개선하는 개선안 제안",
    folder: "코드 품질",
    content: `## 리팩토링

**절대 바꾸지 않는 것**: 외부 동작(API 응답, 반환값), 공개 인터페이스, 테스트 결과.
리팩토링은 동작을 바꾸는 게 아니라 코드를 더 읽기 쉽게 만드는 것이에요.

### 진행 순서

1. **현재 코드 파악** — 하는 일, 진입점, 의존성 목록
2. **문제 식별** — 아래 중 해당하는 것 찾기
   - 중복 로직이 3번 이상 반복됨
   - 함수 하나가 두 가지 이상의 일을 함
   - 이름만 봐서 의도를 알 수 없음
   - 200줄 이상의 파일/함수
   - 깊은 중첩(if 4단계 이상)
3. **가장 안전한 변경부터** — 이름 변경 → 추출 → 이동 순서
4. **검증** — 각 단계마다 타입 체크 + 테스트 통과 확인

### 흔한 패턴

- 조건 분기가 많다면 → 전략 패턴 또는 조기 반환(early return)
- 유사한 함수가 여럿이면 → 공통 추출 후 파라미터로 차이 표현
- 중첩이 깊다면 → 중간 결과를 변수로 추출해 평탄화

리팩토링 후 변경한 부분과 이유를 간략히 요약해요.`,
  },
  {
    slug: "debug",
    name: "버그 디버깅",
    description: "증상 → 재현 → 원인 → 수정 순서로 버그를 분석하고 해결",
    folder: "진단",
    content: `## 버그 디버깅

아래 순서를 건너뛰지 말고 단계별로 진행해요.

### 1단계 — 증상 정확히 파악
- 어떤 입력에서 발생하나요?
- 에러 메시지나 스택 트레이스 전체를 보여주세요.
- 언제부터 발생했나요? (특정 커밋 이후?)

### 2단계 — 재현
- 가장 단순한 재현 케이스를 만들어요.
- 환경 변수, 데이터 상태, 순서 의존성 확인해요.
- "항상 발생"인지 "간헐적"인지 구분해요.

### 3단계 — 원인 찾기
- 스택 트레이스의 가장 안쪽(내 코드) 라인부터 시작해요.
- 가정하지 말고, 실제 값을 출력해서 확인해요.
- 최근 변경된 코드를 우선 의심해요.

### 4단계 — 수정
- 수정 범위를 최소화해요. 버그 하나 = 변경 하나.
- 수정 후 재현 케이스로 검증해요.
- 같은 패턴의 버그가 다른 곳에도 있는지 확인해요.

### 보고 형식
수정 후 아래를 알려주세요:
- **원인**: 왜 발생했는지
- **수정 내용**: 무엇을 바꿨는지
- **재발 방지**: 같은 실수를 막을 방법`,
  },
  {
    slug: "security",
    name: "보안 점검",
    description: "인증 누락, 인젝션, 민감 데이터 노출 등 보안 취약점을 점검하고 개선 방법 제안",
    folder: "진단",
    content: `## 보안 점검

현재 코드를 아래 항목으로 점검해요. 취약점 발견 시 심각도와 수정 방법을 함께 제시해요.

### 입력 검증
- [ ] 모든 사용자 입력에 화이트리스트 또는 길이/형식 검증 있음
- [ ] SQL 쿼리에 파라미터 바인딩 사용 (문자열 결합 없음)
- [ ] HTML 출력 시 이스케이프 처리 (XSS 방지)
- [ ] 파일 업로드 시 확장자·MIME 타입·크기 검증

### 인증 & 인가
- [ ] 모든 보호된 엔드포인트에 인증 체크 존재
- [ ] 리소스 접근 전 소유권 확인 (다른 유저 데이터 접근 불가)
- [ ] 세션/토큰 만료 처리 있음
- [ ] 민감한 엔드포인트에 레이트 리밋 적용

### 비밀 정보
- [ ] 코드에 하드코딩된 시크릿·API 키·비밀번호 없음
- [ ] 로그에 PII·토큰·비밀번호 출력 없음
- [ ] API 응답에 불필요한 민감 데이터 포함 안 됨

### 의존성
- [ ] 알려진 취약점이 있는 패키지 없음 (npm audit / pip check)

### 결과 형식
각 항목을 **통과 / 취약 / 해당없음**으로 표시하고,
취약 항목은 구체적인 파일·라인과 수정 방법을 제시해요.`,
  },
  {
    slug: "test",
    name: "테스트 작성",
    description: "엣지 케이스, 권한, 모듈 간 연동까지 커버하는 테스트 작성",
    folder: "진단",
    content: `## 테스트 작성

아래 레벨을 모두 커버하는 테스트를 작성해요. "페이지가 로드된다"만으로는 부족해요.

### 레벨 1 — 해피 패스 (필수)
정상 입력 → 정상 출력의 기본 흐름을 검증해요.

### 레벨 2 — 엣지 케이스 (필수)
- 빈 값, null, undefined
- 최소/최대 경계값
- 특수문자, 유니코드
- 중복 실행 (같은 요청 두 번)

### 레벨 3 — 실패 경로 (필수)
- 잘못된 입력 → 올바른 에러 반환
- 존재하지 않는 리소스 → 404
- 권한 없음 → 401/403
- 외부 서비스 실패 → 적절한 처리

### 레벨 4 — 권한 (인증이 있는 기능)
- 비인증 사용자 → 거부
- 다른 유저의 리소스 → 거부
- 역할별 접근 범위 검증

### 레벨 5 — 통합 (연결된 모듈)
- A에서 생성 → B에 반영
- A에서 삭제 → B가 안전하게 처리

### 테스트 품질 기준
- 각 테스트는 하나만 검증해요.
- 테스트 이름만 봐도 무엇을 검증하는지 알 수 있어야 해요.
- 외부 의존성은 최소한으로만 모킹해요 (실제와 달라지는 위험 때문).`,
  },
  {
    slug: "explain",
    name: "코드 설명",
    description: "코드가 어떻게 동작하는지 설명해줘요. 누구에게 설명할지 알려주면 맞춰서 설명해요.",
    folder: "진단",
    content: `## 코드 설명

아래 형식으로 코드를 설명해요.
대상 독자를 먼저 알려주시면 적합한 수준으로 조정할게요.
(예: "주니어 개발자에게", "기획자에게", "비개발자에게")

### 설명 구조

**한 줄 요약**
이 코드가 무엇을 하는지 한 문장으로.

**전체 흐름**
1. 입력: 무엇을 받나요?
2. 처리: 어떤 순서로 무엇을 하나요?
3. 출력: 무엇을 반환/저장하나요?

**핵심 개념**
이해하려면 알아야 할 배경 지식이 있다면 간단히 설명해요.
(예: 클로저, 비동기, 트랜잭션 등)

**주의할 점**
- 이 코드가 의존하는 전제 조건
- 잘 작동하지 않는 상황 (한계)
- 향후 변경 시 주의해야 할 부분

기술 용어는 비유로 설명하고, 코드 예시는 최소한으로 사용해요.`,
  },
];
